import json
import joblib

def lambda_handler(event, context):
    # Load the trained model
    model = joblib.load("C:\Users\adeel\OneDrive\Desktop\flask_model_deployment\svr_best_model.pkl")
    
    # Extract input data from the event (e.g., JSON body)
    data = event['body']
    
    # Make predictions using the model
    prediction = svr_best_model.predict(data)
    
    # Return the prediction
    return {
        'statusCode': 200,
        'body': json.dumps({'prediction': prediction.tolist()})
    }
